<UserControl x:Class="gbchef.Controls.SelectableListBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" 
             Orientation="Horizontal"
             VerticalAlignment="Center">

            <Label Content="{Binding Name, RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}}" 
                Margin="5" 
                Width="Auto"
                MinWidth="80"
                HorizontalAlignment="Left"
                FontWeight="Bold"/>
            
            <Button Content="Select All" 
                    Margin="0,0,3,0"
                    Name="SelectAllButton"
                    Click="SelectAllButton_ClickHandler"
                    FontSize="10" Padding="3,2,3,1" BorderBrush="{x:Null}" Background="{x:Null}" Foreground="#FF505050" />
            <Button Content="Clear"
                    Name="ClearButton"
                    Click="ClearButton_ClickHandler"
                    FontSize="10" Padding="3,2,3,1" BorderBrush="{x:Null}" Background="{x:Null}" Foreground="#FF505050" />
        </StackPanel>
        

        <ListBox Grid.Row="1" 
             x:Name="InnerListBox" 
             ItemsSource="{Binding Items, RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}}" MinHeight="20" PreviewMouseLeftButtonDown="InnerListBox_PreviewMouseLeftButtonDown" PreviewMouseMove="InnerListBox_PreviewMouseMove" PreviewMouseLeftButtonUp="InnerListBox_PreviewMouseLeftButtonUp" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSelected}" Value="False">
                                        <Setter Property="Foreground" Value="Black"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsAutoSelected}" Value="True">
                                        <Setter Property="Foreground" Value="Purple"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                        <Setter Property="Foreground" Value="Green"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>


    </Grid>
</UserControl>